// lib/services.ts — Legacy Supabase services stub.
// These are no longer used. All business logic has been migrated to
// the service classes in lib/services/*.service.ts.
// This file remains as a stub to prevent import errors from any existing pages.

export const boardService = {};
export const columnService = {};
export const taskService = {};
export const boardDataService = {};
